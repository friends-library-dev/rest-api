{
  "name": "@friends-library/api",
  "version": "1.0.0",
  "description": "Friends Library APIs",
  "author": "Jared Henderson <jared@netrivet.com>",
  "repository": "github:friends-library-dev/api",
  "private": true,
  "license": "MIT",
  "scripts": {
    "start": "netlify dev --functions ./functions",
    "build": "netlify functions:build --src ./functions --functions ./built-functions",
    "deploy": "mkdir -p ./build && npm run build && netlify deploy --dir=./build --functions=./built-functions # --prod",
    "predeploy": "npm run ensure-not-symlinked # <-- don't bundle dev deps",
    "ensure-not-symlinked": "test ! -L ./node_modules/@friends-library/friends",
    "test": "true",
    "lint": "fldev lint",
    "lint:fix": "fldev lint:fix",
    "ts:check": "fldev ts:check",
    "format": "fldev format"
  },
  "dependencies": {
    "@friends-library/env": "^1.1.0",
    "@friends-library/friends": "^1.1.1"
  },
  "devDependencies": {
    "@friends-library/dev": "^4.0.8"
  }
}
